{
  "name": "totm-convex-mcp",
  "version": "1.0.0",
  "description": "A simple MCP server to connect with totm-convex",
  "main": "index.js",
  "type": "module",
  "bin": {
    "totm-convex-mcp": "./build/index.js"
  },
  "scripts": {
    "build": "tsc && chmod 755 build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "sync:claude-config": "node scripts/update-claude-config.mjs",
    "build-and-sync": "npm run build && npm run sync:claude-config"
  },
  "files": [
    "build"
  ],
  "author": "Conor Meldrum",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.0",
    "convex": "^1.27.0",
    "dotenv": "^16.4.7",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3"
  }
}